# API_URL = "http://0.0.0.0:80"  # when the api service is running from docker container
# API_URL = "http://127.0.0.1:8000"  # when running locally
API_URL = "http://api:80"  # when docker-compose is used
